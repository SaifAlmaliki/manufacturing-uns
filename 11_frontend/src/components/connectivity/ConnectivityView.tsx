import React from 'react';

import { PageShell } from '../ui/console-ui';

export const ConnectivityView: React.FC = () => (
  <PageShell id="connectivity-view" scroll={false} className="flex flex-col">
    <div className="flex flex-1 items-center justify-center p-8 text-sm text-zinc-500">
      Assets &amp; Connectivity — coming in Task 8.
    </div>
  </PageShell>
);
